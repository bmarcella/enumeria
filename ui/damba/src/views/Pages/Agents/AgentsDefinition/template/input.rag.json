{
  "type": "object",
  "required": ["knowledgeScope"],
  "properties": {
    "knowledgeScope": {
      "type": "string",
      "enum": ["project_only", "org_shared", "external_docs"],
      "description": "Defines what knowledge the agent can access."
    },
    "maxChunks": {
      "type": "integer",
      "minimum": 1,
      "maximum": 50,
      "default": 10
    },
    "citationRequired": {
      "type": "boolean",
      "default": true
    }
  },
  "additionalProperties": false
}